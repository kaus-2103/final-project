<div class="container my-5">
  <div class="row">

    
    
    <!-- Collections Section -->
    <div class="card <%= cookies[:theme] %>">
      <h3 class="card-header <%= cookies[:theme] %>">Collections</h3>
      <div class="table-responsive">
        <table class="table <%= cookies[:theme] == 'dark' ? 'table-dark' : '' %> table-borderless table-hover mb-0">
          <thead class="table-dark">
            <tr>
              <th>Name</th>
              <th>Description</th>
              <th>Category</th>
              <th><%= t('action') %></th>
            </tr>
          </thead>
          <tbody>
            <% @collections.each do |collection| %>
              <tr>
                <td><%= collection.name %></td>
                <td><%= collection.description %></td>
                <td><%= collection.category %></td>
                <td>
                  <%= link_to t('edit'), edit_collection_path(collection.id), class: 'btn btn-secondary btn-sm me-2' %>
                  <%= button_to t('delete'), collection_path(collection.id), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="w-500 h-100 mb-2"></div>

    <!-- Items Section -->
    <div class="card <%= cookies[:theme] %>">
      <h3 class="card-header <%= cookies[:theme] %>">Items</h3>
      <div class="table-responsive">
        <table class="table <%= cookies[:theme] == 'dark' ? 'table-dark' : '' %> table-borderless table-hover mb-0">
          <thead class="table-dark">
            <tr>
              <th>Name</th>
              <th>Description</th>
              <th>Category</th>
              <th><%= t('action') %></th>
            </tr>
          </thead>
          <tbody>
            <% @items.each do |item| %>
              <tr>
                <td><%= item.name %></td>
                <td><%= item.description %></td>
                <td><%= item.category %></td>
                <td>
                  <%= link_to t('edit'), edit_collection_item_path(item.collection, item), class: 'btn btn-secondary btn-sm me-2' %>
                  <%= button_to t('delete'), collection_item_path(item.collection, item), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    
  </div>
</div>
